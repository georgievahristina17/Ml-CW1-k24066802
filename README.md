# Machine Learning CW1 (Regression)

This repository contains my CW1 solution for predicting `outcome` from tabular features.

## Structure
- `notebooks/cw1_step_by_step.ipynb` — main workflow (EDA → preprocessing → model selection → tuning → predictions)
- `requirements.txt` — Python dependencies

## Data
The datasets are included in this repository.

## How to run (Notebook)
1. Create and activate a virtual environment
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
